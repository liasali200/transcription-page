css
add breakpoints, media queries for a iPhone size document

javascript
- drag image functionality
- *the data table at the bottom must be removed.*

- *A toggle show/hide button which is always visible. If the toggle button is clicked, the other buttons (brightness, contrast, etc) become visible. The idea is to reduce as much as possible the screen dimensions of the image viewer. Possibly position the main toggle show/hide button at one corner of the image, imposed on top of the image.*

*Two event listeners: 
one to hide the viewer, the other to show the viewer. 

This setting will be permanent (will persist beyond the browser being closed). 
Use localstorage to preserve the setting.*

*All buttons should be larger, 
with tooltips showing a description when mouse hovers over the button.*